[metadata]
name = "dataset_version_creation"
version = "1.0"
description = "This pipeline processes data for X."
type = "DATASET_VERSION_CREATION"

[execution]
pipeline_script = "pipeline.py"
requirements_file = "pyproject.toml"
parameters_class = "utils/parameters.py:ProcessingParameters"

[docker]
image_name = "picsellia/dataset-version-creation"
image_tag = "latest"
cpu = "4"
gpu = "0"
